run_name: gpt-1b
platform: r1z1
gpu_type: a100_80gb
gpu_num: 8
image: mosaicml/examples:llm-latest
optimization_level: 0
# Easy setup of dependencies and execution environment
integrations:
  - integration_type: wandb
    # The Weights and Biases project name
    project: wandb-model-registry-demo
    # The username or organization the Weights and Biases project belongs to
    entity: mosaic-ml
  # - integration_type: git_repo
  #   git_repo: mosaicml/examples
  #   git_branch: matthew/demo  
command: |

  # cd examples

  # pip install ".[llm]" -q

  # pip install --ignore-installed git+ssh://git@github.com/growlix/composer.git@wandb-artifact-model-registry -q

  cd examples/llm

  composer main.py yamls/mosaic_gpt/1b.yaml